<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bashkir.auto_school">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".AutoSchoolApplication"
        android:icon="@drawable/car"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoSchool"
        android:allowBackup="true"
        tools:ignore="AllowBackup">
<!--        android:fullBackupContent=""-->

<!--        <activity-->
<!--            android:name=".start.TestComposeActivity"-->
<!--            android:exported="false"-->
<!--            android:label="@string/title_activity_test_compose"-->
<!--            android:theme="@style/Theme.AutoSchool.NoActionBar" />-->
        <activity android:name=".activities.StudentActivity" />
        <activity android:name=".activities.AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>